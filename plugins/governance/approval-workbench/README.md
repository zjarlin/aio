# Approval Workbench

Sample child plugin mounted under `platform.permission-core` through the `permission.approvalUI` extension point.

It demonstrates the UI contribution boundary for runtime permission approvals: render and decide approval requests without replacing Permission Core or bypassing consent persistence.

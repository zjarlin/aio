# Audit File Sink

Sample child plugin mounted under `platform.permission-core` through the `permission.auditSink` extension point.

It demonstrates the intended boundary for governance plugins: extend audit output without replacing Permission Core or broadening permission decisions.

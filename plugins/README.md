# AIO Built-in Plugin Capsules

This directory contains the built-in `plugin-formula/v1` capsules that describe the current business features.

The first implementation keeps existing Vue pages and Rust command handlers in place, but routes their metadata through the platform registry. The formula files are the source of truth for contributed menus, commands, views, capabilities and plugin-tree relationships.

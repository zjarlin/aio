macOS Automation Sample demonstrates guarded macOS-only automation metadata without enabling direct system automation.

The sample exists to verify platform matrix, provider registration and Permission Core visibility for high-risk macOS automation. It intentionally exposes only a preview provider descriptor; real AppleScript or Accessibility execution must be added behind explicit consent and audit controls.

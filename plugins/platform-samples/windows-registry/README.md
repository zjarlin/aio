Windows Registry Sample demonstrates guarded Windows-only Registry metadata without enabling direct Registry mutation.

The sample verifies platform matrix, provider registration and Permission Core visibility for a high-risk Windows capability. It stays read-only and descriptor-only until a trusted provider, approval UI and audit path are added.

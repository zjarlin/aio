Web Fallback Sample demonstrates a read-only plugin path for Web and Remote shells when native providers are unavailable.

The plugin contributes only a declarative view contract and no sensitive capabilities. It is intentionally safe to render in constrained hosts where the desktop Capability Broker is absent or degraded.

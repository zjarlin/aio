export const webFallbackSample = {
  id: 'platform-samples.web-fallback',
  view: {
    content:
      'This read-only fallback view avoids native providers and can render in Web, Remote and Desktop shells.',
    schema: 'markdown',
  },
};

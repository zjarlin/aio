# aio-front

基于 `adui-dioxus` 的 AIO 前端原型。

当前先落一张 `资产文件` 工作台页：

- 顶部主轴 domain axis
- 左侧模块树
- 中间资产表格
- 右侧上下文面板

运行：

```bash
cargo run -p aio-front
```

输出：

```text
target/aio-front/index.html
```

测试：

```bash
cargo test -p aio-front
```

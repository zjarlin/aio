[application]
# 该名称对应 Cargo 包与 Dioxus 构建产物目录，属于构建边界。
name = "az-aio-app"
default_platform = "web"
out_dir = "dist"
asset_dir = "assets"

[web.app]
title = "AIO"
base_path = "/app"

[web.watcher]
reload_html = true
watch_path = ["src", "plugins/studio/src", "Cargo.toml", "Dioxus.toml"]
